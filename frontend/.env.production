# 운영에서는 CloudFront의 /api/* behavior를 통해 백엔드를 호출한다.
# 프론트와 API를 같은 origin으로 제공해 CORS에 의존하지 않는다.
VITE_USE_MOCK=false
VITE_API_BASE_URL=/api

# 비우면 현재 접속한 CloudFront origin으로 공유 링크를 만든다.
VITE_SHARE_LINK_ORIGIN=
