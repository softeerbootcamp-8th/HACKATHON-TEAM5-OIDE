# 운영에서는 Nginx Proxy Manager로 공개한 백엔드 API를 직접 호출한다.
# 프론트와 API의 origin이 다르므로 백엔드 CORS에 프론트 origin을 허용해야 한다.
VITE_USE_MOCK=false
VITE_API_BASE_URL=https://nb.f1race.site/api

# 비우면 현재 접속한 CloudFront origin으로 공유 링크를 만든다.
VITE_SHARE_LINK_ORIGIN=
