interface:
  display_name: "PR 등록"
  short_description: "이슈와 1:1로 연결된 GitHub PR을 안전하게 등록"
  default_prompt: "$create-pr을 사용해 현재 이슈 브랜치의 PR을 dev 대상으로 등록해 주세요."

policy:
  allow_implicit_invocation: false
